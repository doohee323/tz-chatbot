# Frontend build env example (CI/local). CoinTutor / tz-drillquiz 동일 방식.
# Put in project root as env-frontend; in CI sed CHAT_GATEWAY_API_KEY_PLACEHOLDER (and DOMAIN_PLACEHOLDER if used).

# TZ-Chat Gateway URL (no trailing slash)
VUE_APP_CHAT_GATEWAY_URL=https://chat.drillquiz.com
# TZ-Chat Gateway API Key (CI: replace CHAT_GATEWAY_API_KEY_PLACEHOLDER at build time)
VUE_APP_CHAT_GATEWAY_API_KEY=CHAT_GATEWAY_API_KEY_PLACEHOLDER
# System ID (e.g. drillquiz, cointutor). Must match gateway ALLOWED_SYSTEM_IDS
VUE_APP_CHAT_GATEWAY_SYSTEM_ID=drillquiz
